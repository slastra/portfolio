# Optional. A classic or fine-grained GitHub PAT with public-repo read scope.
# Without it: 60 unauthenticated requests/hour per server IP (fine for low traffic).
# With it: 5000 requests/hour.
GITHUB_TOKEN=
